apiVersion: v1
kind: Secret
metadata:
  name: ssl-certs
  namespace: oam-loxilb
  labels:
    app: oam-loxilb
    component: ssl
    environment: development
type: Opaque
data:
  # These will be populated by the certificate generation script
  # server.crt: (base64 encoded certificate)
  # server.key: (base64 encoded private key)
  # ca.crt: (base64 encoded CA certificate)